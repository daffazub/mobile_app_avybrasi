<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VybrasiApp">

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".KatalogActivity" />
        <activity android:name=".DetailProdukActivity"/>
        <activity android:name=".AdminDashboardActivity"/>
        <activity android:name=".AdminPayoutListActivity" />
        <activity android:name=".AffiliateDashboardActivity"/>
        <activity android:name=".LogAdminActivity" />
        <activity android:name=".AdminPayoutDetailActivity" />
        <activity android:name=".ForgotPasswordActivity"/>
        <activity android:name=".AffiliateListActivity" />
        <activity android:name=".AffiliateDetailActivity" />
        <activity android:name=".RoleLauncherActivity" />
        <activity android:name=".TambahAffiliateActivity" />

    </application>

</manifest>